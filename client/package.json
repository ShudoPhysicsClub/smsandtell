{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild src/main.ts --bundle --outfile=dist/main.js --servedir=. --serve=5173 --watch",
    "build": "esbuild src/main.ts --bundle --minify --target=es2020 --sourcemap --outfile=dist/main.js",
    "build:prod": "esbuild src/main.ts --bundle --minify --target=es2020 --outfile=dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.28.0"
  },
  "dependencies": {
    "typescript": "^6.0.2"
  }
}
